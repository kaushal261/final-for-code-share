/**
 * LOCAL DEVELOPMENT ONLY.
 *
 * On Netlify, assets/js/config.js is auto-generated by build.js from
 * environment variables (SUPABASE_URL, SUPABASE_ANON_KEY) — never commit
 * real credentials there.
 *
 * To run this project locally without Netlify:
 *   1. Copy this file to assets/js/config.js
 *   2. Fill in your own Supabase project URL + anon key below
 *   3. assets/js/config.js is already gitignored so you won't commit it
 */
window.SUPABASE_CONFIG = {
  url: 'https://YOUR-PROJECT-REF.supabase.co',
  anonKey: 'YOUR-SUPABASE-ANON-PUBLIC-KEY'
};
